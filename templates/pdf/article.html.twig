<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ article.titre }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .metadata {
            font-size: 14px;
            color: #666;
            margin-bottom: 20px;
        }
        .category {
            display: inline-block;
            background-color: #f0f0f0;
            padding: 5px 10px;
            border-radius: 3px;
            margin-bottom: 20px;
        }
        .content {
            line-height: 1.6;
        }
        .content img {
            max-width: 100%;
            height: auto;
            margin: 10px 0;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #999;
            text-align: center;
            border-top: 1px solid #eee;
            padding-top: 10px;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="title">{{ article.titre }}</div>
        <div class="metadata">
            Auteur: {{ article.username }} | Date: {{ "now"|date("d/m/Y") }}
        </div>
        <div class="category">{{ article.categorie }}</div>
    </div>
    
    <div class="content">
        {{ article.content|raw }}
    </div>
    
    {% if article.imageName %}
    <div class="image">
        <img src="{{ asset('uploads/articles/' ~ article.imageName) }}" alt="Image de l'article">
    </div>
    {% endif %}
    
    <div class="footer">
        Document généré par SahaCare - Tous droits réservés
    </div>
</body>
</html>
